# CHANGELOG — 계약 표면 변경 로그

**계약 표면**(templates의 파일명·섹션 헤더·상태값 등 commands/*.md가 이름으로 참조하는 것 + 세션 진입 시 자동 수행되는 커맨드 행동)이 바뀔 때만 한 줄 기록 (일반 변경은 git log로 충분). `/harness` §1 싱크 스텝이 이 파일을 읽는다.

형식: `날짜 — 무엇이 바뀜 — 기존 프로젝트 조치: 없음 | 매핑 한 줄 | 이관 | 기타 한 줄`. 한 항목이라도 `templates/` 본문이 바뀌었으면 조치는 "없음"일 수 없다 — 항목을 쪼개거나 조치를 명시한다.

- 2026-07-10 — 하네스 싱크 도입: `templates/CLAUDE.md`에 `## harness 싱크` 섹션(last-sync 마커 + 네이밍 매핑) 신설 — 조치: 첫 싱크 때 루트 CLAUDE.md에 섹션 자동 추가
- 2026-07-27 — Opus 5 라우팅 개편: `/harness` §1 싱크 선언식→자동 체크 + 산출물 위생 스캔, §4에 모델·effort 라우팅 표 흡수(`CLAUDE-FABLE-5.md` 폐기), `/workflow-ops` 신설, `/opus-guide`에 Opus 5 분기(§1.5 델타), implementer `effort: high` — 조치: 다음 `/harness` 진입 시 자동 싱크가 `last-sync:` 마커 신설을 제안(루트 HANDOFF·날짜 suffix 산출물이 있으면 상태 정정·`/tidy` 제안 포함); 타 머신은 pull 후 `./apply.sh --global` 재실행(신규 `/workflow-ops` 심링크)
- 2026-07-29 — second brain 동기 슬롯: `templates/docs/RESEARCH_SPEC.md`에 `## 0. 현재 방향 (second brain 동기용)` 신설 + `/harness` §5에 "thesis 움직였으면 §0 갱신" 한 줄 — 조치: 다음 싱크 때 **빈 슬롯만** 삽입(placeholder `{...}` 포함 = §1 불가침의 '형식' 변경이라 저촉 ❌, **내용 작성은 그 프로젝트 세션 몫** — 싱크가 방향을 추측해 채우지 말 것). vault 미설치 머신은 빈 채로 두면 됨(읽는 쪽이 없을 뿐 무해)
- 2026-08-04 — 세션 계약 재배치: 세션 종료 계약·종료 보고(verdict 1줄+기승전결)·AFK push를 `/opus-guide` §2·§6·§7 → `/harness` §3·§5로 이관(모델 무관 계약 — 7/27 개편 후 Opus 5+/Fable 세션에 미로드되던 회귀 수정), 진입 싱크는 조건부(CHANGELOG에 last-sync보다 새 항목 있을 때만)·진입 시 산출물 위생 스캔 제거(`/tidy` 몫), `/harness` §4 Workflow 기본 고려의 Fable 한정 해제, 외부 리뷰 2라운드 confirm 규칙 §4로 이동, opus-guide 구§8→§7 — 조치: 없음(커맨드 symlink 즉시 반영, templates 불변)
- 2026-08-08 — `/issue` 신설: 뫼비우스(고민·수정 순환) 박제 → vault `fable/issues/YYYY-MM-DD_{프로젝트}_{slug}.md` (+ vault CLAUDE.md에 fable/ 레이어 등재), `/harness` §2에 제안 트리거 1줄 — 조치: 타 머신은 pull 후 `./apply.sh --global` 재실행(신규 심링크), vault도 pull
- 2026-08-08 — 경계선(HARD/SOFT) 정본 이관: `/opus-guide` §1 Boundary Map → `/harness` §경계선(모델 무관 공통 계약, 압축 이관) + README에 사용자용 요약. `/opus-guide` §1은 포인터 스텁, Fable 세션은 opus-guide 로드 불필요로 명시 — 조치: 없음(커맨드 symlink 즉시 반영)
- 2026-08-08 — 아이디어 채널 대칭화: `/harness` §2 💡 인박스에 Claude 제안 착지 허용(`[claude]` 태그+근거 1줄, 세션당 1~2개, 착지≠채택), §3 Setup confirm에 최강 대안 1개 동봉, progress.md 템플릿 착지 규칙 ⑤ 추가 — 조치: 커맨드는 즉시 반영. 다음 싱크 때 기존 progress.md 착지 규칙 주석에 ⑤(`[claude]` 태그+근거 1줄) 추가
- 2026-08-08 — docs 위생 2종: `/harness` §2에 교훈 즉시 착지(사용자 지적 → 그 턴에 LEARNINGS 1줄 append + 쓴 줄 표시, LEARNINGS 템플릿 헤더에도 명시), `/tidy` §5 stale 검사 신설(현행 문서 안 낡은 줄 — 문서 간 대조 판정, confirm 후 반영) — 조치: 커맨드는 즉시 반영. 다음 싱크 때 기존 `docs/LEARNINGS.md` 헤더에 즉시-append 문장 반영
- 2026-08-08 — fresh 리뷰 일괄 수리 15건: 리뷰 라운드 정본 통일(done/_GUIDE·템플릿 → `/harness` §4), 전역 우선순위에 커맨드 계층 삽입, 묵시 적용 §1–§5 통일(+`/issue` 재시도 세션 예외), vault 경로 fallback, `/harness` §1에 references pending 스캔·진행 중 plan 적재 복원, 당일 싱크 재발동 종결 조건, 경계선⑦ 실패 정의 내장, §3 autoloop push 예외(LOOP 우선), `/tidy` 정규 집합에 LOOP.md, stale 정리 confirm 명시, `/issue` 트리거 수락 명시, CHANGELOG 날짜순 복구 — 잔여 소형 7건은 `BACKLOG.md` — 조치: 다음 싱크 때 프로젝트 done 규약(`done/_GUIDE.md` 또는 폴더 `CLAUDE.md`)의 "리뷰어가 더 이상 할 말이 없을 때까지" 줄을 `/harness` §4 포인터(자동 반복 ❌·2라운드 후 confirm)로 교체, `_done_template` §5도 동일
- 2026-08-08 — `/close` 신설: 세션 마감 검증 — 사용자가 "세션 close" 선언 시 이 대화를 모르는 fresh 서브에이전트가 목표 대비 산출물을 적대 검토한 뒤 §5 정리 수행(writer≠reviewer의 세션판), `/harness` §5에 트리거 1줄 — 조치: 타 머신은 pull 후 `./apply.sh --global` 재실행(신규 심링크)
- 2026-08-08 — 2차 최종 검증 수리 7건: `last-sync`를 날짜 → 마지막 반영 항목 마커로(당일 다중 항목 영구 누락 홀 봉합 — templates/CLAUDE.md 형식 동반 변경), 묵시 적용 범위에 §경계선 명시(전역·템플릿), opus-guide 우선순위에 커맨드 계층 전파, `/close` 제안·수락 동형화 + 검토 입력을 plan 전체로 정정, docs/_GUIDE 구명칭 "(b)" 정정, 💡 평가 금지에 전역 push-back 예외 명시 — 잔여 소형 7건 `BACKLOG.md` 추가 — 조치: 다음 싱크 때 ① last-sync 형식 갱신 ② 루트 CLAUDE.md 묵시 적용 문장 "§1-§4" → "본문 전체(§경계선 포함 — 종료 계약 포함)" ③ docs 가이드(`docs/_GUIDE.md` 또는 폴더 `CLAUDE.md`)의 구명칭 "(b)" → "(a)/(experiment)" 정정
- 2026-08-08 — 소급 등기(2026-07-10 CHANGELOG 신설 전 계약 표면 — 위치 비교 싱크가 볼 수 없던 것들): `done/_GUIDE` §Kill/Pivot 4게이트 · progress.md §결정 큐+아이디어 인박스(+착지 규칙 주석) · `docs/LOOP.md` 신설 · plans/_GUIDE 프레이밍("1 가설 = 1 세션 = 1 터미널") · `docs/CLAUDE_AGENTS.md` 폐기(→ `/harness` §4) · references status 사다리 pending→summarized→blueprinted + `summary_path` 컬럼 · `_plan_template` §3(성공 임계값·정지 조건·루프 예산) · `_done_template` §2(seeds×rollouts·mean±std). 동반: `/harness` §1에 부재 섹션 폴백 1줄(커맨드가 이름으로 참조하는 섹션·파일이 프로젝트에 없으면 신설 제안) — 조치: 다음 싱크 때 프로젝트 사본에 위 섹션·파일 실재 대조(2026-07-10 이전 설치 프로젝트는 전부 미반영일 가능성 — 특히 폐기된 `docs/CLAUDE_AGENTS.md` 생존 여부 확인)
- 2026-08-08 — 업스트림 제보 수리(efficient-retarget-v1 싱크발 6건): CHANGELOG 조치 4건 정정+헤더 규약 신설, `/harness` §1 불가침 한정(폴더 가이드는 싱크 대상 ✓)·§3 절 이름 정본 규칙(plan §번호는 템플릿 기준 보조), `/close` 목표 참조 절 이름 기반 정정, `/tidy` §5 대상에 폴더 가이드 추가, `docs/_GUIDE` 문서 목록에 `LEARNINGS.md` 등재, `/add-ref` append 전 헤더 컬럼 확인, references status 정의 정본을 `_GUIDE.md`로 일원화(`_INDEX.md` 헤더는 포인터) — 조치: 다음 싱크 때 ① docs 가이드 문서 목록에 LEARNINGS 줄 실재 확인 ② references `_INDEX.md` 헤더 status 줄에 정본 포인터 반영(프로젝트가 템플릿 밖 status 값·컬럼을 쓰면 그 정의를 references 규약본에 등재)
- 2026-08-10 — 연산 자원 규율 신설(당일 OOM 사고발 — 워크플로 서브에이전트의 CPU 폴백 벤치가 58GB를 잡아 데스크톱 스코프 전체가 kill, 이웃 Claude 세션 8개 동사): 전역 `CLAUDE.md` Conventions에 GPU-first + 호스트 RAM 공유 2줄(상한 스코프 `systemd-run --user --scope -p MemoryMax=`), `/workflow-ops` §2에 호스트 RAM 가드(주입 필수 — 코디네이터가 동시 스테이지 수 × 상한을 계산), `/harness` §경계선 SOFT의 백그라운드 run에 상한값 동봉 — 조치: 없음(전역·커맨드 symlink 즉시 반영, templates 불변). 훅 강제는 `BACKLOG.md`로 보류
- 2026-08-13 — 외부 스킬 3종 통합: ① ponytail vendor — `skills/ponytail/SKILL.md`(@ 2ed6c52 핀, MIT) 신설 + `apply.sh --global`에 `~/.claude/skills/` 링크 스텝, `/harness` §3 Execute·implementer에 사다리 앵커(재현성 규약 우위 명시) ② graphify 앵커 — `/harness` §4 Explore에 query-first(`graphify-out/` 있을 때), `/blueprint-ref` §2에 code형 구조 맵 옵션(CLI는 머신당 `uv tool install graphifyy && graphify install`) ③ headroom opt-in 규약 — README(세션 단위 wrap 실험, learn 산출물은 LEARNINGS로 손 이관), README `## vanilla 스킬 활용` → `## 외부 스킬·도구` 개편 — 조치: 타 머신은 pull 후 `./apply.sh --global` 재실행(ponytail 심링크), graphify·headroom CLI 설치는 선택(미설치면 앵커는 자동 무시, templates 불변)
- 2026-08-21 — 보고 스타일 개편(쉬운 말 정본): 전역 `CLAUDE.md` 보고 스타일을 "비전공자 이해 기준"으로 재작성(성공 기준 = 무엇을 했고·지금 상황·다음 결정이 이해되어 사용자가 바로 다음 미션을 줄 수 있는 상태, "3줄 이내" 압축 조항 폐기 → 결론 먼저 + 내용 선택, 하네스 문서 문체의 대화 전염 금지), `/harness` §5 종료 보고에 전역 보고 스타일 우위 1줄 — 조치: 없음(전역·커맨드 symlink 즉시 반영, templates 불변)
